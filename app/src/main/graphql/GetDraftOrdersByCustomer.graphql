
query GetDraftOrdersByCustomer($query: String!) {
    draftOrders(query: $query, first: 200) {
        nodes {
            lineItems(first: 200) {
                nodes {
                    name
                    quantity
                    title
                    variant {
                        id
                        inventoryQuantity
                        selectedOptions {
                            name
                            value
                        }
                        price
                        product {
                            media(first: 1) {
                                nodes {
                                    ... on MediaImage {
                                        image {
                                            url
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
            id
            tags
        }
    }
}